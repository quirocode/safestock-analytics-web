import { NavLink, Outlet, useNavigate } from 'react-router-dom'

const items=[['Dashboard','/dashboard'],['Catalogo','/catalogo'],['Inventario','/inventario'],['Terminal POS','/pos'],['Historial','/ventas'],['Usuarios','/usuarios'],['Configuracion','/configuracion']]
function Layout(){const navigate=useNavigate();const logout=()=>{localStorage.removeItem('safestock_token');localStorage.removeItem('safestock_user');navigate('/')};return <div className="flex min-h-screen bg-gray-50"><aside className="sticky top-0 flex h-screen w-64 shrink-0 flex-col bg-slate-900 p-4 text-white"><div className="border-b border-slate-700 px-2 pb-5"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-blue-300">SafeStock</p><h1 className="mt-2 text-xl font-bold">Analytics</h1></div><nav className="mt-6 flex flex-1 flex-col gap-1">{items.map(([label,to])=><NavLink key={to} to={to} className={({isActive})=>`rounded-lg px-4 py-3 text-sm font-medium transition ${isActive?'bg-blue-600 text-white shadow-md':'text-slate-300 hover:bg-slate-800 hover:text-white'}`}>{label}</NavLink>)}</nav><button onClick={logout} className="rounded-lg border border-slate-700 px-4 py-3 text-left text-sm font-semibold text-slate-200 hover:bg-slate-800">Cerrar sesion</button></aside><div className="min-w-0 flex-1"><header className="sticky top-0 z-20 flex h-16 items-center justify-between border-b border-gray-100 bg-white px-8 shadow-sm"><div><p className="text-sm font-bold text-gray-900">SafeStock Analytics</p><p className="text-xs text-gray-500">Control operativo y antifraude</p></div><span className="rounded-full bg-emerald-50 px-3 py-1 text-xs font-semibold text-emerald-700">Sistema conectado</span></header><main className="p-8"><Outlet/></main></div></div>}
export default Layout
