import { useEffect, useMemo, useState } from 'react'
import { salesApi } from '../application/sales-api'
import { analyticsApi } from '../../monitoring-analytics/application/analytics-api'
import Notification from '../../shared/components/notification'

const money=v=>`S/ ${Number(v||0).toFixed(2)}`
function SalesHistoryPage(){const[sales,setSales]=useState([]);const[filters,setFilters]=useState({search:'',fechaDesde:'',fechaHasta:''});const[canceling,setCanceling]=useState(null);const[reason,setReason]=useState('');const[notice,setNotice]=useState({message:'',type:'success'});const load=async()=>{try{setSales((await salesApi.list({fechaDesde:filters.fechaDesde||undefined,fechaHasta:filters.fechaHasta||undefined})).data.ventas||[])}catch(e){setNotice({message:e.response?.data?.message||'No se cargaron las ventas.',type:'error'})}};useEffect(()=>{salesApi.list({}).then(r=>setSales(r.data.ventas||[])).catch(e=>setNotice({message:e.response?.data?.message||'No se cargaron las ventas.',type:'error'}))},[]);const visible=useMemo(()=>sales.filter(v=>[v.id,v.usuario_nombres,v.usuario_apellidos,v.usuario_correo].some(x=>String(x||'').toLowerCase().includes(filters.search.toLowerCase()))),[sales,filters.search]);const cancel=async e=>{e.preventDefault();try{await salesApi.cancel(canceling.id,reason);setCanceling(null);setReason('');setNotice({message:'Venta anulada y stock restaurado.',type:'success'});load()}catch(x){setNotice({message:x.response?.data?.message||'No se pudo anular.',type:'error'})}};const download=async(id)=>{const response=await salesApi.receipt(id);const url=URL.createObjectURL(response.data);window.open(url,'_blank','noopener,noreferrer');setTimeout(()=>URL.revokeObjectURL(url),60000)};const exportCsv=async()=>{const response=await analyticsApi.exportSales(filters);const url=URL.createObjectURL(response.data);const link=document.createElement('a');link.href=url;link.download='reporte-ventas.csv';link.click();URL.revokeObjectURL(url)};return <section><Notification {...notice}/><div className="mb-6 flex items-end justify-between"><div><p className="text-xs font-semibold uppercase text-blue-600">Auditoria comercial</p><h1 className="mt-2 text-2xl font-semibold">Historial de ventas</h1></div><button onClick={exportCsv} className="rounded-lg border border-blue-200 px-4 py-2 font-semibold text-blue-700">Exportar CSV</button></div><div className="mb-6 grid gap-3 rounded-lg bg-white p-4 shadow-md md:grid-cols-4"><input className="rounded-lg border px-3 py-2 md:col-span-2" placeholder="Buscar por ID o cajero" value={filters.search} onChange={e=>setFilters({...filters,search:e.target.value})}/><input className="rounded-lg border px-3 py-2" type="date" value={filters.fechaDesde} onChange={e=>setFilters({...filters,fechaDesde:e.target.value})}/><div className="flex gap-2"><input className="min-w-0 flex-1 rounded-lg border px-3 py-2" type="date" value={filters.fechaHasta} onChange={e=>setFilters({...filters,fechaHasta:e.target.value})}/><button onClick={load} className="rounded-lg bg-blue-600 px-4 text-white">Filtrar</button></div></div><div className="overflow-hidden rounded-lg bg-white shadow-md"><table className="min-w-full"><thead className="bg-gray-50 text-xs uppercase text-gray-500"><tr>{['Venta','Fecha','Cajero','Total','Estado','Acciones'].map(h=><th key={h} className="px-5 py-3 text-left">{h}</th>)}</tr></thead><tbody>{visible.map(v=><tr key={v.id} className="border-t border-gray-100"><td className="px-5 py-4 font-bold">#{v.id}</td><td className="px-5 py-4 text-sm">{new Date(v.fecha).toLocaleString('es-PE')}</td><td className="px-5 py-4 text-sm">{v.usuario_nombres} {v.usuario_apellidos}</td><td className="px-5 py-4 font-semibold">{money(v.total)}</td><td className="px-5 py-4"><span className={`rounded-full px-2 py-1 text-xs font-semibold ${v.estado==='ANULADA'?'bg-red-100 text-red-800':'bg-emerald-100 text-emerald-800'}`}>{v.estado}</span></td><td className="px-5 py-4 space-x-2"><button onClick={()=>download(v.id)} className="rounded border px-3 py-2 text-xs font-semibold">PDF</button>{v.estado!=='ANULADA'&&<button onClick={()=>setCanceling(v)} className="rounded border border-red-200 px-3 py-2 text-xs font-semibold text-red-700">Anular</button>}</td></tr>)}</tbody></table></div>{canceling&&<div className="fixed inset-0 z-40 flex items-center justify-center bg-slate-950/50 p-4"><form onSubmit={cancel} className="w-full max-w-md rounded-lg bg-white p-6 shadow-xl"><h2 className="text-lg font-bold">Anular venta #{canceling.id}</h2><p className="mt-2 text-sm text-gray-500">El stock sera restaurado. El motivo quedara en auditoria.</p><textarea className="mt-4 min-h-28 w-full rounded-lg border p-3" minLength="10" required value={reason} onChange={e=>setReason(e.target.value)}/><div className="mt-4 flex justify-end gap-2"><button type="button" onClick={()=>setCanceling(null)} className="rounded-lg border px-4 py-2">Cancelar</button><button className="rounded-lg bg-red-600 px-4 py-2 font-semibold text-white">Confirmar anulacion</button></div></form></div>}</section>}
export default SalesHistoryPage
